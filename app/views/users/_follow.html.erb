<div class="d-flex justify-content-center">
  <%= form_with model: current_user.active_relationships.build, class: "follow_form" do |f| %>
    <div><%= hidden_field_tag :followed_id, @user.id %></div>
    <%= f.submit "Follow", class: "btn btn-primary" %>
  <% end %>
</div>
